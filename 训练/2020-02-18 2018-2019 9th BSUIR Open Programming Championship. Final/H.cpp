#include <bits/stdc++.h>

using namespace std;
int _w;

int main() {
	int T;
	_w = scanf( "%d", &T );
	while( T-- ) {
		int a, b;
		_w = scanf( "%d%d", &a, &b );
		puts("1");
	}
	return 0;
}
